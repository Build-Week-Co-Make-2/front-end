import React, {useState, useEffect} from 'react';
import { Card, Button, Form, FormGroup, Label, Input } from 'reactstrap';
import axios from 'axios';

const Login= props => {
    console.log(props)

    return (
        <>
        <Card color="info">
            <h2 style={{color: "white", margin: "0 auto"}}>login</h2>
        </Card>
        </>
    )}

    export default Login;

